module.exports = {
    preset: 'ts-jest',          // Use ts-jest for transforming TypeScript files
    testEnvironment: 'node',    // Set the test environment to Node.js
    testMatch: ['**/Tests/**/*.ts'], // Make sure this matches your test directory
  };